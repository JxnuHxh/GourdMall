export const getName = (state) => {
  return state.name
}

export const getAge = (state) => {
  return state.age
}

export const getCate = (state) => {
  return state.cate
}

export const getProductList = (state) => {
  return state.productList
}
